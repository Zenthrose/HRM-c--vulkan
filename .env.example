# HRM Environment Configuration
# Copy this file to .env and customize paths for your system

# Custom directories (optional - defaults to current directory or user home)
HRM_CONFIG_DIR=~/.hrm/config      # Config files location
HRM_LOG_DIR=~/hrm/logs           # Log files location
HRM_MODEL_DIR=~/hrm/models       # Model checkpoints location
HRM_DATA_ROOT=./data             # Data directory for training corpora

# Cloud storage (optional)
HRM_CLOUD_API_KEY=your_api_key   # For cloud integration (Google Drive, Dropbox, etc.)

# Logging (optional - defaults to info)
HRM_LOG_LEVEL=info               # debug, info, warn, error

# Vulkan SDK (optional - auto-detected)
VULKAN_SDK=/path/to/vulkan/sdk    # If not in standard location

# MinGW Path (Windows optional)
MINGW_PATH=C:/msys64/mingw64     # For MinGW DLL detection

# Temporary directories (optional - uses system defaults)
TMPDIR=/tmp                      # Temporary files location