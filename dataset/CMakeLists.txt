# Dataset generation CMakeLists.txt

# Python scripts for dataset generation
# These are not compiled, just marked as dependencies for testing

# Note: Dataset generation requires Python dependencies
# Run with: python build_arc_dataset.py, python build_maze_dataset.py, etc.